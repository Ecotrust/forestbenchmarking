from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='STAC specification for data collection, transformation, and organization to build and benchmark models for predicting forest attributes.',
    author='Ecotrust',
    license='MIT',
)
